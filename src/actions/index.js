export const setUsers = users => (
    { type: "SET_USERS",
     users: users }
    );

export const setId = id => (
    { type: "SET_ID",
     id: id }
    );

export const setUser = user => (
    { type: "SET_USER",
     user: user}
    );

export const setUpdatedUser = userUpdate => (
    { type: "SET_UPDATED_USER",
     userUpdate: userUpdate}
    );

